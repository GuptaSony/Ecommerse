import { UserAuthenticateInput } from './user-authenticate-input.model';

describe('UserAuthenticateInput', () => {
  it('should create an instance', () => {
    expect(new UserAuthenticateInput()).toBeTruthy();
  });
});
